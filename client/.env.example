# Vercel Deployment & Environment Variables
# If deploying client and server separately on Vercel:
# Set VITE_API_URL to your deployed Vercel backend URL (e.g., https://recipebox-server.vercel.app)
# During local development, leave it empty to automatically use the Vite dev proxy
VITE_API_URL=
